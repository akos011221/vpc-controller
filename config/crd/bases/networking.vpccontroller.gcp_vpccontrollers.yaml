---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: vpccontrollers.networking.vpccontroller.gcp
spec:
  group: networking.vpccontroller.gcp
  names:
    kind: VPCController
    listKind: VPCControllerList
    plural: vpccontrollers
    singular: vpccontroller
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: VPCController is the Schema for the vpccontrollers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VPCControllerSpec defines the desired state of VPCController.
            properties:
              controlled_projects:
                items:
                  type: string
                type: array
              policy_based_routes:
                items:
                  properties:
                    dest_addr:
                      type: string
                    name:
                      type: string
                    next_hop:
                      type: string
                    next_hop_type:
                      type: string
                    source_addr:
                      type: string
                  required:
                  - dest_addr
                  - name
                  - next_hop
                  - next_hop_type
                  - source_addr
                  type: object
                type: array
              subnet_max_size:
                format: int32
                type: integer
            required:
            - controlled_projects
            - policy_based_routes
            - subnet_max_size
            type: object
          status:
            description: VPCControllerStatus defines the observed state of VPCController.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
